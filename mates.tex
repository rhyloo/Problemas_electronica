\mkexercise{Dada la base de Lagrange $B = \{L_0(x),L_1(x),...,L_n(x)\}$ y el
  polinomio $Pn(x)=a_0L_0(x)+...+a_nL_n(x)$. Demostrar que
  $a_0=f(x_0),...,a_n=f(x_n)$}
\mksolution{
  Definimos $L_i(x)$ como:
  \begin{equation}
    L_i(x) = \dfrac{\displaystyle \prod_{i \neq j} \left(a-x_j\right)}{\displaystyle \prod_{i \neq j} \left(x_i-x_j\right)}
  \end{equation}

  Si consideramos una serie de datos de la forma:
  
}